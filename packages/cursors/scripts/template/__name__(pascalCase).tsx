import { CursorIcon } from '../types'

const __name__(pascalCase): CursorIcon = {
  d: '__path__(noCase)',
  paths: [__paths__(noCase)],
  id: '__name__(kebabCase)',
  stroke: '__stroke__(noCase)',
  fill: '__fill__(noCase)',
  x: '__x__(noCase)',
  y: '__y__(noCase)'
}

export default __name__(pascalCase)
