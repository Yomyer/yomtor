import { createStyles } from '@yomtor/styles'
import { SortableProps } from './Sortable.props'

export interface SortableStylesParams {}

export default createStyles((theme, {}: SortableStylesParams) => ({
  root: {}
}))
