import { createStyles } from '@yomtor/styles'
import { __name__(pascalCase)Props } from './__name__(kebabCase).props'

type Classes = 'root'

export const __name__(pascalCase)Styles = createStyles<Classes, __name__(pascalCase)Props>(
    (theme, { color }) => ({
        root: {
            color: color
        }
    })
)
